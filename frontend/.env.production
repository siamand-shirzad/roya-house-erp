# Baked into the bundle at build time. The backend serves the SPA from the same
# origin in production, so a relative /api needs no CORS and keeps the session
# cookie same-site. Dev keeps the default http://localhost:4000/api.
VITE_API_URL=/api
