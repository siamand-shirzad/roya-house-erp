# استقرار روی Liara

اپ به‌صورت **یک برنامه** اجرا می‌شود: سرور Express هم API را می‌دهد و هم فایل‌های build فرانت را سرو می‌کند. یک دامنه، بدون CORS، و کوکی نشست بی‌مشکل.

```
یک برنامه‌ی node  ─┬─ /api/*  → Express
                   └─ سایر مسیرها → frontend/dist (SPA)
                        └─ PostgreSQL (شبکه‌ی خصوصی)
```

## فایل‌های استقرار

| فایل | کارش |
|---|---|
| `package.json` (ریشه) | Liara اسکریپت‌های همین فایل را می‌بیند: `build` هر دو بخش را می‌سازد، `start` سرور را اجرا می‌کند. |
| `liara.json` | پلتفرم `node`، شناسه‌ی برنامه، پورت `3000` — همان `PORT` که Liara خودش به کانتینر می‌دهد. |
| `.liaraignore` | `node_modules`، `dist` و `.git` آپلود نمی‌شوند (Liara خودش نصب و build می‌کند). |
| `frontend/.env.production` | `VITE_API_URL=/api` — هنگام build داخل bundle پخته می‌شود. |

## یک‌بار در پنل Liara

1. **دیتابیس PostgreSQL** بساز. افزونه‌های PostGIS و Pgvector لازم نیست.
2. **برنامه‌ی `node`** بساز. اگر شناسه‌اش `royahouse` نیست، `app` را در `liara.json` عوض کن.
3. برنامه و دیتابیس را در **یک شبکه‌ی خصوصی** بگذار.
4. در متغیرهای محیطی برنامه:

   | متغیر | مقدار |
   |---|---|
   | `DATABASE_URL` | URI **شبکه‌ی خصوصی** دیتابیس (`postgresql://root:...@<db-host>:5432/postgres`) |
   | `NODE_ENV` | `production` |

   `NODE_ENV=production` اختیاری نیست: کوکی نشست فقط در این حالت `secure` می‌شود و `trust proxy` فعال می‌شود (وگرنه محدودکننده‌ی تلاش ناموفق ورود، همه‌ی کاربرها را یک IP می‌بیند).

   `CORS_ORIGIN` لازم نیست — فرانت از همان مبدأ سرو می‌شود.

## استقرار

```bash
npm install -g @liara/cli
liara login
```

⚠️ `liara deploy` به GitHub وصل نیست: **همان فایل‌هایی که همین لحظه در پوشه‌ی پروژه هستند** آپلود می‌شوند، یعنی branch فعال. همیشه از `main` به‌روز deploy کن:

```bash
git checkout main
git pull
git status          # باید تمیز باشد؛ تغییرات commit‌نشده هم آپلود می‌شوند
liara deploy
```

Liara در سرور `npm install` می‌زند، `npm run build` را اجرا می‌کند (نصب و build هر دو بخش) و بعد `npm start`.

موقع بوت، سرور `SCHEMA_SQL` را اجرا می‌کند: جدول‌ها، ستون‌ها و indexهای جدید (`CREATE ... IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS`) خودشان ساخته می‌شوند و **داده‌ی موجود دست نمی‌خورد**. push یا deploy هیچ داده‌ای از دیتابیس لوکال به سرور نمی‌برد.

## بعد از اولین استقرار

1. `/login` را باز کن؛ چون هیچ مدیری وجود ندارد، خودش حالت **ساخت حساب مدیر** را نشان می‌دهد. همان لحظه این کار را بکن.
2. **اطلاعات شرکت** را در منوی Admin → «اطلاعات شرکت» (`/settings/company`) کامل کن: شناسه ملی، شماره اقتصادی، آدرس و تلفن روی همه‌ی اسناد چاپ می‌شوند. ردیف شرکت هنگام بوت خودکار ساخته می‌شود (اگر جدول خالی باشد)؛ INSERT دستی لازم نیست.
3. **فهرست قیمت** را وارد کن: در اپ لوکال «خروجی CSV» بگیر و در اپ روی سرور «ورود از CSV» کن. به دسترسی دیتابیس نیاز ندارد.
4. **`STARTING_NUMBER`** در `backend/src/routes/documents.ts` را با شماره‌ی دفتر کاغذی شرکت تنظیم کن، **قبل از** ساخت اولین سند واقعی.
5. **موجودی اولیه‌ی انبار**: موجودی همه‌ی کالاها از صفر شروع می‌شود. در صفحه‌ی «انبار»، برای هر کالا از منوی ردیف «اصلاح موجودی» را بزن، عدد شمارش‌شده را وارد کن و دلیل را «موجودی اولیه» بنویس. در صورت نیاز «حداقل موجودی» را هم تعیین کن تا کالاهای کم‌موجود روی داشبورد دیده شوند.

⚠️ `npm run seed` را روی دیتابیس سرور اجرا نکن: علاوه بر فهرست قیمت، سیزده سند نمونه و مشتری‌های تستی هم می‌سازد.

## به‌روزرسانی‌های بعدی

- تغییرات را روی یک branch جدا بساز، با PR بررسی و در `main` merge کن، بعد طبق بخش «استقرار» از `main` deploy کن.
- اگر نسخه‌ی جدید جدول یا ستون تازه دارد، لازم نیست کاری بکنی؛ موقع بوت ساخته می‌شود. فقط **تغییر** ستون موجود (نوع، نام، حذف) دستی است؛ بخش «نگه‌داری» را ببین.
- حواله‌هایی که قبل از فعال شدن انبار صادر شده‌اند، از موجودی کسر نشده‌اند و با ابطال هم چیزی برنمی‌گردانند.

## دیدن و ویرایش دیتابیس

**pgAdmin پنل Liara** (ساده‌ترین): در صفحه‌ی دیتابیس، pgAdmin را فعال کن. نسخه‌ی موقتی است و حدود ۵ ساعت بعد حذف می‌شود.
- دیدن داده: `Databases → postgres → Schemas → public → Tables` ← راست‌کلیک روی جدول ← View/Edit Data → All Rows
- SQL: Tools → Query Tool

**از کامپیوتر خودت**: «دسترسی شبکه‌ی عمومی» دیتابیس را موقتاً روشن کن و با DBeaver، pgAdmin دسکتاپ یا psql (مثلاً `docker run --rm -it postgres:16 psql "<URI عمومی>"`) وصل شو. **بعد از کار، دسترسی عمومی را خاموش کن.** آدرس و رمز دیتابیس را هیچ‌جا commit نکن.

قبل از ویرایش دستی:
- بکاپ بگیر.
- هر کاری که صفحه دارد (قیمت، مشتری، اطلاعات شرکت، کاربر، موجودی) را از خود اپ انجام بده؛ ویرایش مستقیم جدول قواعد اپ را دور می‌زند.
- اسناد صادرشده (`documents`، `document_items`)، `stock_movements` (موجودی از جمع آن محاسبه می‌شود)، `sessions` و `users.password_hash` را دستی تغییر نده.
- SQL تغییردهنده را داخل `BEGIN; ... COMMIT;` بزن و قبل از `COMMIT` نتیجه را با `SELECT` چک کن (در صورت اشتباه `ROLLBACK;`). هیچ‌وقت `UPDATE` یا `DELETE` بدون `WHERE` نزن.

## رمز مدیر را فراموش کردی

اپ صفحه‌ی «فراموشی رمز» ندارد و رمزها برگشت‌پذیر نیستند (hash شده‌اند). اگر جایی هنوز با حساب مدیر وارد هستی، از صفحه‌ی «کاربران» رمز را عوض کن. وگرنه:

**ساده‌ترین راه — کنسول خود برنامه در پنل Liara** (آنجا `DATABASE_URL` ست است):

```bash
npm run hash-password -- --apply
```

نام کاربری و رمز جدید را می‌پرسد (رمز موقع تایپ نمایش داده نمی‌شود) و مستقیم روی دیتابیس production می‌نویسد. اگر نام کاربری اشتباه باشد، فهرست نام‌های موجود را چاپ می‌کند. نشست‌های باز آن کاربر هم بسته می‌شوند.

**راه دوم — از کامپیوتر خودت**، وقتی به کنسول دسترسی نداری:

1. در پوشه‌ی پروژه: `npm run hash-password`
2. فایل `reset-password.sql` ساخته می‌شود (در `.gitignore` و `.liaraignore` هست). **آن را در ویرایشگر باز کن**، نه ترمینال: ترمینال خط بلند را می‌شکند و کاراکتر محل شکستن را تکرار می‌کند، و hash خراب کپی می‌شود — بعد `UPDATE 1` می‌گیری ولی ورود باز هم رد می‌شود.
3. محتوایش را در pgAdmin پنل Liara (Query Tool) اجرا کن؛ باید `UPDATE 1` بدهد. بعد فایل را پاک کن.

نکته: رمز را با **کیبورد انگلیسی** تایپ کن. اسکریپت رمزِ دارای حروف غیرانگلیسی را رد می‌کند، چون با کیبورد فارسی چیزی ذخیره می‌شود که موقع ورود قابل تایپ نیست.

خود رمز هیچ‌جا ذخیره یا چاپ نمی‌شود، فقط hash آن. اپ هیچ لحظه بدون مدیرِ دارای رمز نمی‌ماند.

## نگه‌داری

- **بکاپ**: بررسی کن دیتابیس مدیریت‌شده‌ی Liara بکاپ خودکار دارد یا نه؛ اگر نه، با کلید `cron` در `liara.json` یک `pg_dump` شبانه بگذار.
- **تغییر شمای دیتابیس**: مهاجرت نداریم. جدول و ستون **جدید** با `SCHEMA_SQL` خودکار ساخته می‌شوند، اما تغییر یا حذف ستونی که روی دیتابیسِ پرشده وجود دارد یعنی `ALTER TABLE` دستی، به‌علاوه‌ی همان تغییر در `SCHEMA_SQL`.
