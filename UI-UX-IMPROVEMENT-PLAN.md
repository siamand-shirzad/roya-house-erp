# برنامه ادامه توسعه رابط و محصول رویا هاوس

بازبینی‌شده در ۱۴۰۵/۰۶/۲۵ (2026-09-16) بر اساس کد فعلی، build تولید و تست‌های مرورگر.

## وضعیت فعلی

پایه رابط برای استفاده روزانه آماده است: پوسته روشن و تیره، سایدبار واکنش‌گرا، Command Palette انگلیسی، ایجاد سریع، تقویم شمسی، نمایش موبایل، فهرست قیمت قابل ویرایش، فیلتر برند، گردش انبار، دسترسی سطح ماژول و چرخه پیش‌فاکتور تا حواله خروج وجود دارد. فرم فاکتور نیز اکنون ساختار یکپارچه، اطلاعات خریدار خلاصه، جزئیات اختیاری و کنترل بازشدنی تخفیف و مالیات دارد.

ریسک اصلی دیگر کمبود کامپوننت نیست؛ رشد تدریجی دامنه محصول است. پرداخت، برگشت از فروش، مهاجرت دیتابیس و تاریخچه تغییرات باید قبل از اضافه شدن صفحات بیشتر تثبیت شوند.

## اولویت ۱ — تکمیل کار روزانه فروش

### پرداخت و مانده مشتری

- تخصیص دریافت به یک یا چند فاکتور، پرداخت جزئی و برگشت دریافت.
- وضعیت مستقل «تسویه»، «بخشی پرداخت شده» و «سررسید گذشته»؛ وضعیت مالی با وضعیت صدور سند مخلوط نشود.
- نمایش مانده و آخرین دریافت در صفحه مشتری و فاکتور.
- تعریف دقیق روش‌های پرداخت، چک، سررسید و سطح دسترسی قبل از تغییر schema.

**معیار خروج:** جمع تخصیص‌ها از مبلغ فاکتور بیشتر نشود، برگشت دریافت سابقه داشته باشد و گزارش مانده با اسناد و دریافت‌ها تطبیق کند.

### برگشت از فروش و ارسال جزئی

- جایگزینی صفحه فعلی «به‌زودی» با برگشت مبتنی بر ردیف فاکتور/حواله.
- نگهداری مقدار سفارش، ارسال‌شده، برگشتی و باقی‌مانده برای هر ردیف.
- ثبت حرکت معکوس انبار در یک تراکنش و جلوگیری از برگشت بیشتر از مقدار تحویل‌شده.

**معیار خروج:** موجودی و زنجیره سند پس از صدور، ابطال و برگشت قابل محاسبه مجدد باشد.

## اولویت ۲ — اطمینان و نگهداری داده

### مهاجرت نسخه‌دار دیتابیس

اکنون تغییرات schema در startup و با `ALTER ... IF NOT EXISTS` اعمال می‌شوند. برای هر تغییر آینده یک migration شماره‌دار، جدول تاریخچه migration، اجرای تراکنشی و راهنمای rollback لازم است.

**معیار خروج:** یک دیتابیس قدیمی و یک دیتابیس خالی هر دو با همان فرمان به schema یکسان برسند و CI تفاوت schema را تشخیص دهد.

### تاریخچه رویدادها

- ثبت append-only برای تغییر قیمت، اصلاح موجودی، صدور/ابطال سند، reset رمز و تغییر دسترسی.
- ذخیره actor، زمان، موجودیت، مقدار قبل/بعد و دلیل، بدون ثبت رمز یا داده حساس.
- نمایش timeline کوتاه در جزئیات کالا، سند و کاربر.

**معیار خروج:** مدیر بتواند علت تغییر قیمت یا موجودی را بدون بررسی log سرور پیدا کند.

### سخت‌گیری دسترسی و نشست

- تست ماتریسی نقش‌ها و overrideها برای همه routeهای فعلی و قابلیت‌های جدید.
- صفحه نشست‌های فعال و خروج اجباری از دستگاه‌ها بعد از reset رمز یا غیرفعال‌سازی.
- بازبینی سیاست مشاهده اطلاعات مالی و exportها.

## اولویت ۳ — سرعت و مقیاس

### کوچک‌کردن bundle اولیه

build فعلی هنوز درباره chunk بالای 500 kB هشدار می‌دهد. مسیرهای گزارش، PDF، سایت عمومی و صفحات سنگین باید lazy شوند؛ سپس اندازه و زمان اولین تعامل قبل و بعد اندازه‌گیری شود.

**معیار خروج:** هشدار chunk اصلی برطرف شود و بازشدن داشبورد به import کتابخانه PDF وابسته نباشد.

### pagination و summary سمت سرور

- مشتریان و کالاها برای داده زیاد به جستجو، sort و pagination سمت سرور منتقل شوند.
- dashboard و Command Palette از endpointهای summary/search محدود استفاده کنند.
- export مستقل از صفحه جاری و به‌صورت stream یا job برای داده بزرگ انجام شود.

**معیار خروج:** زمان و حجم پاسخ لیست با ۱۰۰ هزار رکورد به تعداد کل رکوردها وابسته نباشد.

### پایداری تست مرورگر

- اسکریپت `npm run lint` تعریف شده ولی ESLint در devDependencies/config پروژه موجود نیست؛ lint باید نصب و در CI اجباری شود.
- Playwright اکنون پایان child process در Windows را گاهی دیر تشخیص می‌دهد؛ webServer lifecycle و teardown باید جداگانه اصلاح شوند.
- تست drag انبار در اجرای گروهی گاهی flaky است؛ رویداد keyboard reorder باید با یک تست مستقیم state و یک تست مرورگر کم‌نوسان پوشش داده شود.
- تست تصویری سبک برای هدر موبایل، فرم Bottom Sheet، Products و فاکتور اضافه شود.

## اولویت ۴ — یکپارچگی طراحی

- ساخت یک `DataTable` مشترک برای selection، sort، column visibility، pagination و حالت موبایل؛ جدول مشتریان الگوی اولیه است.
- استخراج `PageToolbar` و `FilterBar` مشترک تا فیلترهای Products، Inventory، اسناد و پرداخت رفتار و ارتفاع یکسان داشته باشند.
- افزودن density انتخابی برای جدول‌های عملیاتی: Comfortable در موبایل و Compact در دسکتاپ.
- استاندارد کردن Bottom Sheet موبایل برای create/edit و Command dialog دسکتاپ، با بازگشت focus و دکمه ذخیره همیشه قابل دسترس.
- محدودکردن Geist Pixel Square به اعداد و داده‌های عددی؛ متن فارسی با فونت خوانای فعلی بماند.

## ترتیب پیشنهادی تحویل

| مرحله | خروجی | وابستگی |
|---|---|---|
| ۱ | migration نسخه‌دار و audit log پایه | قبل از schema قابلیت‌های مالی |
| ۲ | پرداخت و مانده مشتری | قواعد مالی تأییدشده |
| ۳ | برگشت و ارسال جزئی | مدل ردیف و حرکت انبار |
| ۴ | DataTable و toolbar مشترک | بدون تغییر قرارداد API |
| ۵ | pagination/search سمت سرور | indexهای دیتابیس و اندازه داده واقعی |
| ۶ | route splitting و بودجه عملکرد | baseline مرورگر و bundle |

## چک‌لیست هر انتشار

- build frontend و backend، integration دیتابیس و Playwright نقش‌های ADMIN، SALES، WAREHOUSE و ACCOUNTANT.
- عرض 360 و 390 موبایل، لپ‌تاپ 1280×720، نمایش 200٪ و هر دو پوسته.
- create → save → issue → revise/convert → dispatch → cancel با قطع شبکه و تعارض هم‌زمان.
- PDF با ۱، ۱۷ و ۴۰ ردیف، نام فارسی بلند، مالیات/تخفیف و عددهای بزرگ.
- بررسی overflow افقی، focus keyboard، target حداقل 44px موبایل و حالت کاهش motion.
